# 极简桌面头部验收

日期：2026-09-11。使用 image-to-code 工作流，仅实施用户批准的头部，不重新设计核心界面。

参考：本任务生成的 `exec-dcafe79d-6fee-4290-9e23-45b6be329aac.png`（1536×1024）；仅比较顶部白色产品条，不包含说明文字与展示画布。
实机：Windows release，默认客户区 1280×840；截图 `output/design-qa/header-runtime.jpg`。另检查了最大化窗口。

## 视觉检查

- 布局：通过。左侧品牌、中央留白拖动区、右侧更多与窗口控件；独立 48px 头部。
- 层级：通过。没有常驻文字菜单，没有可见原生标题栏。
- 色彩与边界：白底、细灰分隔线；核心自身浅色/深色主题保持独立。
- 图标：使用项目现有官方鲸鱼 SVG 和 Lucide 图标，不复制生成图中的近似品牌图形。
- 核心保真：不修改核心 DOM、CSS 或内容；独立子 WebView 从头部下方开始，最大化与还原时同步尺寸。

## 功能检查

- Windows release 构建与 NSIS 打包成功。
- 实机核心启动成功；更多图标展开、最大化、还原已检查。
- 启动及核心运行窗口快照中未出现独立终端窗口。
- Rust 子进程使用 CREATE_NO_WINDOW；Node 预加载覆盖 child_process 常用 API 及自定义 promisify 适配器，保留 stdio。
- 隐藏窗口回归测试 3 项通过，Node 22 与本机 Node 的真实子进程输出测试通过。
- 修复实机测试发现的 NODE_OPTIONS Windows 反斜杠转义问题。

## 验证边界

Windows 已完成上述检查；macOS/Linux、混合 DPI、拖动和最小化尚未完整实机验收。不声称对任意第三方程序主动创建的 GUI/控制台窗口作全局拦截。未执行真实聊天任务或覆盖所有外部工具调用。多 WebView 使用 Tauri unstable 功能，升级时需回归。
